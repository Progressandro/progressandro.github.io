import React from 'react';
import { Typography } from '@material-ui/core';

const ProjectCarousel = () => {
  return <Typography variant="h1">Carousel</Typography>;
};

export default ProjectCarousel;
